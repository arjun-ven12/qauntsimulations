# TaskOS backup demo plan

Use the strongest existing evidence-backed checkout Investigation. Do not start a new Daytona run, Kimi planning request, hosted repair verification, or deployment as a fallback.

## If the Dashboard activity feed is available

Open the latest completed `Checkout Reliability Lab` Investigation, then its confirmed duplicate-submission Finding. Show the persisted Environment, bounded Worlds, evidence, reproductions, and final report.

## If the Dashboard activity feed is unavailable

1. Explain that dashboard configuration remains available while the optional activity feed is unavailable.
2. Open `Checkout Reliability Lab` through the Project experience and show the READY `Local Demo Store`, checkout Journey, and enabled Invariants.
3. Use an already-known, browser-visible Investigation or Finding link from the active Product session. Do not construct a URL from a database ID.
4. If no existing navigation is available in the session, present the verified Product configuration and explain that the activity API needs to recover before a live evidence walkthrough can continue.

## If Repair Verification is requested

Show the Finding's **Verify repair** entry point and the preflight workflow only. A selected Environment and eligible bounded plan are preparation, not a completed business result. Do not claim a hosted verification has run while Daytona outbound HTTPS remains blocked.

## Truthful presenter language

Use: “The public Render fixture is verified; the Daytona sandbox lifecycle is verified; the evidence-backed Investigation used the reachable Local Demo Store path.”

Use: “Daytona-to-Render execution is blocked by the Daytona organisation's outbound-network policy.”

Do not use: “The hosted repair passed,” “the defect is fixed,” or “the hosted Investigation completed.”
