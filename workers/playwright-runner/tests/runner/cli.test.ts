import { describe, expect, it } from 'vitest'; import { EXIT_CODES, exitCodeForResult, parseArguments } from '../../src/cli.js'; describe('CLI', () => { it('maps result statuses to documented exit codes', () => { expect(exitCodeForResult('PASSED')).toBe(0); expect(exitCodeForResult('INVARIANT_VIOLATION')).toBe(2); expect(exitCodeForResult('FAILED')).toBe(3); expect(exitCodeForResult('TIMED_OUT')).toBe(4); expect(exitCodeForResult('RUNNER_ERROR')).toBe(6); }); it('requires --job', () => { expect(() => parseArguments([])).toThrow(); expect(EXIT_CODES.INVALID_JOB).toBe(5); }); });
