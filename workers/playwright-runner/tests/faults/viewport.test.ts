import { describe, expect, it } from 'vitest'; import { resolveViewport } from '../../src/browser/browser-factory.js'; describe('viewport presets', () => { it('resolves desktop and mobile', () => { expect(resolveViewport('desktop')).toEqual({ width: 1440, height: 900 }); expect(resolveViewport('mobile')).toEqual({ width: 390, height: 844 }); }); it('retains a validated custom viewport', () => expect(resolveViewport({ width: 800, height: 600 })).toEqual({ width: 800, height: 600 })); });
