import { describe,expect,it } from 'vitest'; import { createAIProvider } from '../provider-factory.js';
describe('createAIProvider',()=>{it('uses the mock when OpenAI credentials are absent',()=>expect(createAIProvider({provider:'openai',openai:{baseUrl:'https://api.openai.com/v1',plannerModel:'planner',explanationModel:'explain',visionModel:'vision'}}).name).toBe('MOCK'));it('creates OpenAI when configured',()=>expect(createAIProvider({provider:'openai',openai:{apiKey:'test',baseUrl:'https://api.openai.com/v1',plannerModel:'planner',explanationModel:'explain',visionModel:'vision'}}).name).toBe('OPENAI'))});
