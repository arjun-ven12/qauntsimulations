import type { SandboxHandle, SandboxProvider } from '../integrations/daytona/daytona.types.js'; export class CleanupExpiredSandboxesJob { constructor(private readonly sandbox: SandboxProvider) {} async run(expired: SandboxHandle[]): Promise<void> { await Promise.all(expired.map((handle) => this.sandbox.deleteSandbox(handle))); } }
