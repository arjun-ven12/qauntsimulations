import type { Readable } from 'node:stream'; import type { EvidenceStorage, StoredEvidence } from './evidence-storage.interface.js'; export class ObjectStorageServiceStub implements EvidenceStorage { private unavailable(): never { throw new Error('Object storage adapter is not implemented'); } put(_key: string, _content: Buffer | Readable): Promise<StoredEvidence> { return Promise.reject(this.unavailable()); } get(_key: string): Promise<Readable> { return Promise.reject(this.unavailable()); } delete(_key: string): Promise<void> { return Promise.reject(this.unavailable()); } exists(_key: string): Promise<boolean> { return Promise.reject(this.unavailable()); } }
