import type { EvidenceStorage } from './evidence-storage.interface.js'; import { LocalEvidenceStorage } from './local-evidence-storage.service.js'; import { ObjectStorageServiceStub } from './object-storage.service.stub.js'; export function createEvidenceStorage(provider: 'local' | 'object', localPath: string): EvidenceStorage { return provider === 'local' ? new LocalEvidenceStorage(localPath) : new ObjectStorageServiceStub(); }
