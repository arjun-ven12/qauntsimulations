import { Router } from 'express'; import { validateBody } from '../../core/validation/validate.js'; import type { ScenarioController } from './scenarios.controller.js'; import { createScenarioSchema } from './scenarios.schema.js'; export function createScenarioRouter(controller: ScenarioController) { const router = Router({ mergeParams: true }); router.get('/', controller.list); router.post('/', validateBody(createScenarioSchema), controller.create); return router; }
