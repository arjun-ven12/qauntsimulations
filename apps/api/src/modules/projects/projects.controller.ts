import type { NextFunction, Request, Response } from 'express';
import type { ProjectService } from './projects.service.js';
export class ProjectController { constructor(private readonly service: ProjectService) {} create = async (request: Request, response: Response, next: NextFunction) => { try { response.status(201).json(await this.service.create(request.auth!.organisationId!, request.body)); } catch (error) { next(error); } }; list = async (request: Request, response: Response, next: NextFunction) => { try { response.json(await this.service.list(request.auth!.organisationId!)); } catch (error) { next(error); } }; get = async (request: Request, response: Response, next: NextFunction) => { try { response.json(await this.service.get(request.auth!.organisationId!, String(request.params.projectId))); } catch (error) { next(error); } }; }
