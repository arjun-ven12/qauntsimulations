export function mapJourney(record: { id: string; projectId: string; name: string; description: string | null; createdAt: Date; updatedAt: Date; steps: Array<{ id: string; order: number; action: string; selector: string | null; value: string | null; metadata: unknown }> }) { return { ...record, steps: record.steps.map((step) => ({ ...step, metadata: step.metadata as Record<string, unknown> })), createdAt: record.createdAt.toISOString(), updatedAt: record.updatedAt.toISOString() }; }
