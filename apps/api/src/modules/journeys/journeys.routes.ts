import { Router } from 'express'; import { validateBody } from '../../core/validation/validate.js'; import type { JourneyController } from './journeys.controller.js'; import { createJourneySchema } from './journeys.schema.js'; export function createJourneyRouter(controller: JourneyController) { const router = Router({ mergeParams: true }); router.get('/', controller.list); router.post('/', validateBody(createJourneySchema), controller.create); return router; }
