import type { WorldConfig } from '@taskos/shared-types'; export interface MinimisationCandidate { world: WorldConfig; removedFactors: string[] } export class GreedyMinimisationService { candidates(world: WorldConfig): MinimisationCandidate[] { return [{ world: { ...world, injectedFaults: [], worldId: `${world.worldId}-no-faults`, reason: 'Test whether injected faults are required.' }, removedFactors: ['injectedFaults'] }, { world: { ...world, concurrency: 1, worldId: `${world.worldId}-single-user`, reason: 'Test whether concurrency is required.' }, removedFactors: ['concurrency'] }]; } }
