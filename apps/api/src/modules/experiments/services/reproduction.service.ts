import type { WorldConfig } from '@taskos/shared-types'; export interface ReproductionStrategy { worlds(failingWorld: WorldConfig, count?: number): WorldConfig[] } export class ExactReproductionService implements ReproductionStrategy { worlds(world: WorldConfig, count = 3) { return Array.from({ length: count }, (_, index) => ({ ...world, worldId: `${world.worldId}-reproduction-${index + 1}`, reason: 'Exact deterministic reproduction run.' })); } }
