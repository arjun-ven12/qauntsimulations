import { Router } from 'express'; import { validateBody } from '../../core/validation/validate.js'; import type { EnvironmentController } from './environments.controller.js'; import { createEnvironmentSchema } from './environments.schema.js'; export function createEnvironmentRouter(controller: EnvironmentController) { const router = Router({ mergeParams: true }); router.get('/', controller.list); router.post('/', validateBody(createEnvironmentSchema), controller.create); return router; }
