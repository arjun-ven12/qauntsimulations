import { describe,expect,it } from 'vitest'; import { canTransitionInvestigation } from '../investigation-status.js'; describe('investigation status transitions',()=>{it('allows planned lifecycle steps',()=>expect(canTransitionInvestigation('PLANNING','PLAN_READY')).toBe(true));it('prevents terminal investigations restarting',()=>expect(canTransitionInvestigation('COMPLETED','RUNNING')).toBe(false))});
