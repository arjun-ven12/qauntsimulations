export class MemoryTrustEvaluator { evaluate(input: { evidenceStrength: number; reproductionCount: number; repairVerified: boolean; applicationVersionMatches: boolean }): number { const reproduction = Math.min(1, input.reproductionCount / 3); return Math.min(1, input.evidenceStrength * 0.45 + reproduction * 0.25 + (input.repairVerified ? 0.2 : 0) + (input.applicationVersionMatches ? 0.1 : 0)); } }
