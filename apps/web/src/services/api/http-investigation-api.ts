import { investigationSchema, projectSchema, findingSchema } from '@taskos/shared-types'; import { z } from 'zod'; import type { CreateInvestigationRequest, InvestigationApi } from './investigation-api.js';
export class HttpInvestigationApi implements InvestigationApi { constructor(private readonly baseUrl: string) {} private async request(path: string, init?: RequestInit): Promise<unknown> { const response = await fetch(`${this.baseUrl}${path}`, { credentials: 'include', headers: { 'Content-Type': 'application/json', ...init?.headers }, ...init }); const payload = await response.json(); if (!response.ok) throw new Error((payload as { error?: { message?: string } }).error?.message ?? 'API request failed'); return payload; } async listProjects() { return z.array(projectSchema).parse(await this.request('/projects')); } async createProject(input: { name: string; description: string | null; repositoryUrl: string | null }) { return projectSchema.parse(await this.request('/projects', { method: 'POST', body: JSON.stringify(input) })); } async createInvestigation(input: CreateInvestigationRequest) { const { projectId, ...body } = input; return investigationSchema.parse(await this.request(`/projects/${projectId}/investigations`, { method: 'POST', body: JSON.stringify(body) })); } async getInvestigation(id: string) { return investigationSchema.parse(await this.request(`/investigations/${id}`)); } async listFindings(id: string) { return z.array(findingSchema).parse(await this.request(`/investigations/${id}/findings`)); } }
