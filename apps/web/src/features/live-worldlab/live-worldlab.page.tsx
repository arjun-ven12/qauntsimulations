import { useQuery } from '@tanstack/react-query'; import { useParams } from 'react-router-dom'; import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis } from 'recharts'; import { PageHeading } from '../../components/page-heading.js'; import { investigationApi } from '../../services/api/index.js';
export function LiveWorldLabPage() { const {investigationId='investigation-demo'}=useParams(); const {data}=useQuery({queryKey:['investigation',investigationId],queryFn:()=>investigationApi.getInvestigation(investigationId),refetchInterval:2000}); if(!data)return <div>Connecting to the lab…</div>; const counts=Object.entries(data.workerCounts).map(([name,value])=>({name,value})); return <><PageHeading eyebrow={data.status} title="Live WorldLab" description="Polling execution state now; the event contract can later back SSE or WebSockets."/><div className="mb-5 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full bg-cyan" style={{width:`${data.aggregateProgress}%`}}/></div><div className="grid gap-4 lg:grid-cols-2"><section className="card"><h2 className="font-bold">Worker activity</h2><div className="mt-4 h-52"><ResponsiveContainer width="100%" height="100%"><BarChart data={counts}><XAxis dataKey="name" stroke="#64748b"/><Tooltip/><Bar dataKey="value" fill="#41d9e8" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="card"><h2 className="font-bold">Recent events</h2><ol className="mt-4 space-y-4">{data.recentEvents.map((event)=><li key={event.id} className="border-l-2 border-cyan/40 pl-4"><div className="font-medium">{event.type.replaceAll('_',' ')}</div><time className="text-xs text-slate-500">{new Date(event.occurredAt).toLocaleTimeString()}</time></li>)}</ol></section></div></>; }
