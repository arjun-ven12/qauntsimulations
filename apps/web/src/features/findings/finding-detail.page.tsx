import { Link, useParams } from 'react-router-dom'; import { PageHeading } from '../../components/page-heading.js'; export function FindingDetailPage(){const {findingId}=useParams();return <><PageHeading eyebrow="Confirmed finding" title="Duplicate payment after impatient resubmission" description={`Finding ${findingId ?? ''} reproduced in three deterministic worlds.`}/><div className="grid gap-4 lg:grid-cols-[1fr_340px]"><section className="card"><h2 className="font-bold">Minimum reproduction</h2><ol className="mt-4 list-decimal space-y-3 pl-5 text-slate-300"><li>Open the checkout with one item.</li><li>Delay the payment response by 2,200 ms.</li><li>Submit again after 300 ms.</li><li>Observe two captured payments for one order.</li></ol><pre className="mt-5 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-cyan">paymentDelayMs: 2200{`\n`}retryIntervalMs: 300{`\n`}doubleSubmit: true</pre></section><aside className="card h-fit"><div className="eyebrow">Causal boundary</div><p className="mt-3 text-sm text-slate-400">The control world without double submission passes. Lower and higher latency worlds reproduce while the retry overlaps the payment window.</p><Link to="/repairs/repair-demo/verify" className="mt-5 block rounded-lg bg-cyan px-4 py-2 text-center font-bold text-ink">Verify a repair</Link></aside></div></>}
