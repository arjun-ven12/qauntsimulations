import { useQuery } from '@tanstack/react-query'; import { ArrowRight, Plus } from 'lucide-react'; import { Link } from 'react-router-dom'; import { PageHeading } from '../../components/page-heading.js'; import { investigationApi } from '../../services/api/index.js';
export function ProjectsPage() { const {data=[]}=useQuery({queryKey:['projects'],queryFn:()=>investigationApi.listProjects()}); return <><PageHeading eyebrow="Workspace" title="Projects" description="Systems and environments available for safe counterfactual testing." action={<Link className="rounded-lg bg-cyan px-4 py-2 font-bold text-ink" to="/projects/new"><Plus className="mr-2 inline" size={17}/>New project</Link>}/><div className="grid gap-4 md:grid-cols-2">{data.map((project)=><Link key={project.id} to={`/projects/${project.id}`} className="card group"><div className="eyebrow">Commerce target</div><h2 className="mt-3 text-xl font-bold">{project.name}</h2><p className="mt-2 text-sm text-slate-400">{project.description}</p><span className="mt-6 inline-flex items-center gap-2 text-sm text-cyan">Open project <ArrowRight size={15} className="transition group-hover:translate-x-1"/></span></Link>)}</div></>; }
