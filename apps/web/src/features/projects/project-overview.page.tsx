import { Link, useParams } from 'react-router-dom'; import { FlaskConical, Globe2, Route } from 'lucide-react'; import { PageHeading } from '../../components/page-heading.js'; export function ProjectOverviewPage() { const {projectId}=useParams(); return <><PageHeading eyebrow="Demo Store" title="Project overview" description="A safe target configured for checkout and inventory experiments." action={<Link className="rounded-lg bg-cyan px-4 py-2 font-bold text-ink" to={`/projects/${projectId}/investigations/new`}>New investigation</Link>}/><div className="grid gap-4 md:grid-cols-3"><Metric icon={<Globe2/>} label="Environment" value="Demo · localhost:5174"/><Metric icon={<Route/>} label="Journey" value="Checkout · 6 steps"/><Metric icon={<FlaskConical/>} label="Investigations" value="1 active"/></div></>; } function Metric({icon,label,value}:{icon:React.ReactNode;label:string;value:string}) { return <div className="card"><span className="text-cyan">{icon}</span><div className="mt-5 text-xs uppercase tracking-widest text-slate-500">{label}</div><div className="mt-1 font-semibold">{value}</div></div>; }
