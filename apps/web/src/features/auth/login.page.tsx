import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { Button } from '@taskos/ui'; import { useAuthStore } from '../../stores/auth.store.js';
export function LoginPage() { const [email,setEmail]=useState('demo@taskos.local'); const signIn=useAuthStore((s)=>s.signIn); const navigate=useNavigate(); return <AuthShell title="Welcome back" subtitle="Continue your reliability investigations."><form className="space-y-4" onSubmit={(event)=>{event.preventDefault();signIn(email);navigate('/projects')}}><label className="block text-sm text-slate-400">Email<input data-testid="email" className="mt-1 w-full" type="email" value={email} onChange={(e)=>setEmail(e.target.value)}/></label><label className="block text-sm text-slate-400">Password<input data-testid="password" className="mt-1 w-full" type="password" defaultValue="worldlab-demo"/></label><Button className="w-full">Sign in</Button><p className="text-center text-sm text-slate-500">New here? <Link className="text-cyan" to="/register">Create an account</Link></p></form></AuthShell>; }
export function RegisterPage() { return <AuthShell title="Create your lab" subtitle="Start with a protected organisation workspace."><form className="space-y-4"><input className="w-full" placeholder="Your name"/><input className="w-full" placeholder="Organisation"/><input className="w-full" placeholder="Email"/><input className="w-full" type="password" placeholder="Password (12+ characters)"/><Button className="w-full">Register</Button></form></AuthShell>; }
function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}) { return <div className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#12304a,#07111f_50%)] p-5"><section className="card w-full max-w-md p-8"><div className="eyebrow">TaskOS WorldLab</div><h1 className="mt-3 text-3xl font-black">{title}</h1><p className="mb-7 mt-2 text-slate-400">{subtitle}</p>{children}</section></div>; }
