import { test,expect } from '@playwright/test'; test.beforeEach(async({request})=>{await request.post('/test/reset')}); test('completes the controlled checkout',async({page})=>{await page.goto('/products/sku-1');await page.getByTestId('add-to-cart').click();await page.getByTestId('checkout').click();await page.getByTestId('place-order').click();await expect(page.getByTestId('order-confirmation')).toBeVisible()});
